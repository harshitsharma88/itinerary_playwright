{
  "name": "itinerary-playwright",
  "version": "1.0.0",
  "description": "PDF generation service using Playwright",
  "main": "server.js",
  "scripts": {
  "build": "npx playwright install chromium",
  "start": "node server.js"
},
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.40.0",
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
